<Window x:Class="Chat.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:local="clr-namespace:Chat"
        xmlns:userControls="clr-namespace:Chat.Views.UserControls"
        xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
        mc:Ignorable="d"
        Height="720" Width="1200"
        Style="{StaticResource WindowWithoutDefaultStyle}">

    <Grid Background="{StaticResource MainBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <userControls:TitleBar Grid.Row="0" Background="Transparent" ButtonsForeground="White"/>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center">
            <TextBlock Margin="20" Foreground="White" HorizontalAlignment="Center" FontSize="48">Sign in</TextBlock>
            <svgc:SvgViewbox Source="Images/logo.svg" MaxHeight="90"/>

            <Grid Margin="0,20">
                <Border Margin="0, 20" CornerRadius="12" BorderThickness="2" BorderBrush="{StaticResource LoginWindowBorderBrush}">
                    <Rectangle Fill="#161b22" Height="320" Width="500" RadiusX="12" RadiusY="12"/>
                </Border>

                <StackPanel Margin="30">
                    <TextBlock Margin="0,15,0,0" Foreground="White" FontSize="24" HorizontalAlignment="Left">Login</TextBlock>
                    <ui:TextBox Margin="0, 12" Foreground="White" FontSize="18" Background="{StaticResource MainBackground}" BorderBrush="{StaticResource LoginWindowBorderBrush}" />

                    <TextBlock Margin="0, 12" Foreground="White" FontSize="24" HorizontalAlignment="Left">Password</TextBlock>
                    <userControls:ClearablePasswordBox Foreground="White" FontSize="18" Background="{StaticResource MainBackground}" BorderBrush="{StaticResource LoginWindowBorderBrush}"/>

                    <ui:Button HorizontalAlignment="Stretch" Background="Green" Appearance="Success" Foreground="White" BorderBrush="{StaticResource LoginWindowBorderBrush}" Margin="0, 35, 0, 0">
                        <TextBlock FontSize="20" Text="Sign in"/>
                    </ui:Button>
                </StackPanel>
            </Grid>

            <!--<Grid>
                <Border CornerRadius="12" BorderThickness="2" BorderBrush="{StaticResource LoginWindowBorderBrush}">
                    <Rectangle Fill="Transparent"  Height="120" Width="500" RadiusX="12" RadiusY="12"/>
                </Border>
                
                <StackPanel Margin="12" Orientation="Vertical">
                    <ui:Button HorizontalAlignment="Center" Appearance="Dark"  Background="Transparent">
                        <TextBlock FontSize="22" Foreground="#3a93f8" Text="Forgot password?"/>
                    </ui:Button>
                    <ui:Button Margin="0,5" HorizontalAlignment="Center" Appearance="Dark"  Background="Transparent">
                        <TextBlock FontSize="22" Foreground="#3a93f8" Text="Create an account"/>
                    </ui:Button>
                </StackPanel>
            </Grid>-->

        </StackPanel>
    </Grid>
    
</Window>
