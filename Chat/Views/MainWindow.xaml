<Window x:Class="Chat.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chat"
        xmlns:tabItems="clr-namespace:Chat.Views.TabItems"
        xmlns:userControls="clr-namespace:Chat.Views.UserControls"
        mc:Ignorable="d"
        Height="720" Width="1200"
        MinHeight="540" MinWidth="480"
        Style="{StaticResource WindowWithoutDefaultStyle}">
    
    <Grid Background="{DynamicResource MainBackground}">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <userControls:TitleBar Grid.Row="0" 
                       Background="Transparent" ButtonsForeground="White">
        </userControls:TitleBar>

        <TabControl Grid.Row="1">
            
            <TabControl.Resources>
                <Style x:Key="HiddenControl" TargetType="{x:Type Control}">
                    <Setter Property="Visibility" Value="Hidden"/>
                    <Setter Property="Height" Value="0"/>
                </Style>
            </TabControl.Resources>

            <tabItems:LoginTabItem Style="{StaticResource HiddenControl}" IsSelected="True"/>
        </TabControl>
    </Grid>
    
</Window>
