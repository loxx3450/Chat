<UserControl x:Class="Chat.Views.UserControls.ClearablePasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Chat.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid x:Name="PassBoxGrid" Background="Transparent" 
          GotFocus="PassBoxGrid_GotFocus" LostFocus="PassBoxGrid_LostFocus">
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="36"/>
        </Grid.ColumnDefinitions>

        <Border x:Name="PassBoxBorder" Grid.ColumnSpan="2" BorderThickness="1" CornerRadius="3.6">
            <Rectangle x:Name="PassBoxBackground" RadiusX="3.6" RadiusY="3.6"/>
        </Border>

        <PasswordBox x:Name="PassBox" Grid.ColumnSpan="2" CaretBrush="White" 
                     PasswordChanged="PassBox_PasswordChanged"/>

        <Button x:Name="ClearTextBtn" Grid.Column="1" Visibility="Collapsed" 
                Click="ClearTextBtn_Click">
            <ui:SymbolIcon Symbol="Dismiss16" Foreground="White"/>
        </Button>
    </Grid>
</UserControl>
