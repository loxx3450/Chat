<UserControl x:Class="Chat.MVVM.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Chat.MVVM.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.33*" MinWidth="240"/>
            <ColumnDefinition Width="3"/>
            <ColumnDefinition Width="0.67*" MinWidth="400"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0"
              Background="{DynamicResource SecondaryBackground}">
            
        </Grid>

        <GridSplitter Grid.Column="1" 
                      VerticalAlignment="Stretch" 
                      HorizontalAlignment="Stretch" 
                      Background="{DynamicResource ChatGridSplitter}"/>

        <Grid Grid.Column="2" 
              Background="{DynamicResource MainBackground}">
            
        </Grid>
        
    </Grid>
    
</UserControl>
